# Site access password (required — the server will not start without it).
ACCESS_PASSWORD=changeme

# Optional: explicit secret for signing session/share tokens.
# If unset, it is derived from ACCESS_PASSWORD.
# AUTH_SECRET=
