# Site access password. Optional — falls back to "modal" when unset, so set this
# to something real before exposing the app publicly.
ACCESS_PASSWORD=changeme

# Optional: explicit secret for signing session/share tokens.
# If unset, it is derived from ACCESS_PASSWORD.
# AUTH_SECRET=
